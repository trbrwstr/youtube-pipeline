api_base = "https://api.openai.com/v1"
api_key = "${OPENAI_API_KEY}"
model = "gpt-4o-mini"
timeout_secs = 30
max_concurrency = 3
max_title_chars = 100
max_desc_chars = 5000
max_tags = 15
channel_blurb = "Forgotten Classics — public-domain books, distilled.\n\nNew videos weekly. Subscribe for more dead authors, live ideas."